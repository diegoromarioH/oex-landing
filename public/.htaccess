<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Preserve real files and directories, and route every client-side URL
  # through the Vite/React entry point.
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
